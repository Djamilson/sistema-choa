const VideoLength = () => {
  /* const videoLengthInSeconds = moment()
        ?.startOf('day')
        ?.seconds(time)
        ?.format('H:mm:ss') */
  return (
    <span className="absolute bottom-2 right-2  bg-black px-2 py-1 text-xs text-white">
      videoLengthInSeconds
    </span>
  )
}

export { VideoLength }
