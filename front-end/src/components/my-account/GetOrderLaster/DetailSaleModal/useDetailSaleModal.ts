export const useDetailSaleModal = () => {
  /*
  const onSubmit: SubmitHandler<ValidationSchema> = async (data) => {
    try {
      if (!address?.id || !location?.cep) {
        Toast({
          message: 'Erro ao tentar cadastrar o endereço, tente novamente!',
          type: 'error',
        })
        reset()
        onCloseModal()
        return
      }

      const id = address.id

      const newAddress = await updateAddress.mutateAsync({
        ...data,
        id,
        zip_code: location?.cep,
        number: Number(data.number),
      })

      upDateAddress({
        ...newAddress,
        zipCodeFormat: formatZipCodeInPoint(newAddress.zip_code),
      })

      Toast({
        message: 'Endereço de entrega cadastrado com sucesso!',
        type: 'success',
      })
      reset()
      onCloseModal()
    } catch (err) {
      Toast({
        message: 'Erro ao tentar cadastrar o endereço, tente novamente!',
        type: 'error',
      })
    }
  } */

  return {}
}
