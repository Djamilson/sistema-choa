export type IAddress = {
  id: string
  main: boolean
  street: string
  number: number
  complement?: string
  zip_code: string
  zipCodeFormat?: string
  receiver_id: string
  neighborhood: string
  city: string
  state: string
  status: boolean
}
